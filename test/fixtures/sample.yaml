# Sample YAML file similar to what is tied to an organization on taskless.io
pack: 1
name: "@taskless/test"
version: "1.0.0"
description: Test pack with basic functionality expressed
sends:
  durationMs:
    type: "number"
    description: "The duration of the request in milliseconds"
  status:
    type: "number"
    description: "The status code of the request"
rules:
  - matches: "https://.+"
    hooks:
      pre: |
        context.set("start", now())
      post: |
        send("durationMs", now() - context.get("start"))
        send("status", response.getStatus())
