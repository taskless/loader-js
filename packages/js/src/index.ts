console.warn(
  JSON.stringify({
    level: "warn",
    message:
      "The default import is not available. Please import a library specific interceptor",
  })
);

const empty = {};

export default empty;
